# Leave unset in dev: requests use /api and Vite proxies to the backend (no CORS issues).
# For production builds, set to your API origin, e.g. https://api.example.com
# VITE_API_URL=http://127.0.0.1:8000
